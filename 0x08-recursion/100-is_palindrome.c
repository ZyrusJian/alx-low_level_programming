#include "main.h"

/**
 * is_palindrome - compute if a string is a palindrome
 * @*s: is a string
 *
 * Return: 1 if palindrome other wise 0
 */

int is_palindrome(char *s)
{
	if (*s == "\0")
		return (1);
	if (*s != "\0")
		
	return (x * _pow_recursion(x, y - 1));
}
