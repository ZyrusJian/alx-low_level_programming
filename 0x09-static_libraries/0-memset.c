#include <unistd.h>

/**
 * _memset - writes the characters c to stdout
 * @s: The characters to print
 * @b: param2
 * @n: param3
 *
 * Return: On success 0 .
 *
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
