#include <unistd.h>

/**
 * _strpbrk - writes the characters c to stdout
 * @s: The characters to print
 * @accept: param2
 *
 * Return: On success 0.
 *
 */
char *_strpbrk(char *s, char *accept)
{
	return (0);
}
