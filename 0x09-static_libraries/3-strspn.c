#include <unistd.h>

/**
 * _strspn - writes the characters c to stdout
 * @s: The characters to print
 * @accept: param2
 *
 * Return: On success 0.
 *
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
