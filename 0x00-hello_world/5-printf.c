/*
 * this program uses the printf() function to print
 * a string exactly followed by a newline.
*/
#include<stdio.h>
/**
 * main - displays a string
 * Return: o for succes.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
